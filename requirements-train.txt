# Training environment: laptop dev, CI smoke-train. Everything the pipeline,
# tests, and bundle/state export scripts need. NONE of this belongs in the
# serving image -- see requirements-serve.txt and ARCHITECTURE.md §3 for the
# dependency-isolation rule this split exists to enforce.
joblib==1.5.3
numpy==2.4.6
pandas==2.3.3
scikit-learn==1.9.0
scipy==1.17.1
threadpoolctl==3.6.0
PyYAML==6.0.3
psutil==7.2.2
duckdb==1.5.5
xgboost==3.2.0
lightgbm==4.7.0
optuna==4.9.0
mlflow==3.14.0
shap==0.51.0
numba==0.66.0
llvmlite==0.48.0
slicer==0.0.8

# Sprint 3 additions
pandera==0.32.1
pyarrow==24.0.0
pytest==9.1.1
